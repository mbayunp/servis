import { Request, Response, NextFunction } from 'express';

export const uploadMiddleware = (req: Request, res: Response, next: NextFunction) => {
  // Placeholder for multer upload configuration
  next();
};
